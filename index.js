// Require external resources
var Word = require("./Word.js");
var prompt = require("prompt");

// Start the prompt 
prompt.start();

// Declare global variables
var gameBank = ["peanut", "sienna", "oafie"];